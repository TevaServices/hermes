@AGENTS.md

This repo's working instructions live in AGENTS.md (imported above) — the
deployment model (Komodo Stack `hermes`), the Build-then-Deploy rule for
image changes, secrets layout, and the 1-CPU right-sizing constraints. For
the control plane itself, see the komodo repo's AGENTS.md.